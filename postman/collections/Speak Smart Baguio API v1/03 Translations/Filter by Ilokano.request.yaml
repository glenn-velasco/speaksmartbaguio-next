$kind: http-request
url: "{{base_url}}/api/v1/translations?ilokano=test"
method: GET
queryParams:
  ilokano: test
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status 200", function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 3000
