$kind: http-request
url: "{{base_url}}/api/v1/phrasebook?id={{phrasebook_id}}"
method: DELETE
queryParams:
  id: "{{phrasebook_id}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status 200", function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 4000
