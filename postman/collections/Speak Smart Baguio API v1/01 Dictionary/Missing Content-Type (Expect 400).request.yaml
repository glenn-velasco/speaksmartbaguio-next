$kind: http-request
url: "{{base_url}}/api/v1/dictionary"
method: POST
body:
  type: text
  content: '{"ilokanoWord":"test"}'
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status 400", function () {
        pm.response.to.have.status(400);
      });
    language: text/javascript
order: 10000
